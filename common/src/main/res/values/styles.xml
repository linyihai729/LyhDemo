<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- 没有标题状态 -->
    <style name="CommonMainTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/player_color_primary</item>
        <item name="colorPrimaryDark">@color/player_color_primarydark</item>
        <item name="colorAccent">@color/player_color_accent</item>
        <item name="android:windowBackground">@color/player_windows_bg</item>
    </style>

    <!--自定义dialog背景全透明无边框theme -->
    <style name="progress_common_dialog" parent="Widget.AppCompat.ProgressBar">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
    <!--自定义dialog背景全透明无边框theme -->
    <style name="common_dialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="dialog_select_anim">
        <item name="android:windowEnterAnimation">@anim/dialog_select_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_select_exit</item>
    </style>

    <style name="dialog_common_anim">
        <item name="android:windowEnterAnimation">@anim/dialog_common_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_common_exit</item>
    </style>
    <!-- dialog -->
    <style name="hx_app_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>
</resources>
